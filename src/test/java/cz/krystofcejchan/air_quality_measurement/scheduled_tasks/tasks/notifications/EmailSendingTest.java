package cz.krystofcejchan.air_quality_measurement.scheduled_tasks.tasks.notifications;

import org.junit.jupiter.api.Test;

//@RunWith(SpringRunner.class)
class EmailSendingTest {
    /*
        @TestConfiguration
        static class EmployeeServiceImplTestContextConfiguration {
            @Bean
            public EmailServiceImpl emailService() {
                return new EmailServiceImpl();
            }
        }
        @Autowired
        private EmailServiceImpl emailService;
    */
    @Test
    void sendTestEmails() {
        // var emailDetails = new EmailDetails("TEST", "TEST EMAIL VERIFICATION", "krystofcejchan@seznam.cz");
        //  var httpStatus = emailService.sendSimpleMail(emailDetails);
        //System.out.println(httpStatus);
    }
}
